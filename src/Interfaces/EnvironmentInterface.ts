/**
 * The interface for the initial environment object
 */
export interface EnvironmentInterface {
    APP_VERSION: string,
    APP_BUILD: number,
    PORT: string | number,
    SERVICE_NAME: string
}